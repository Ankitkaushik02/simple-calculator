# 🧮 Simple Calculator

A basic Python program that performs simple arithmetic operations — addition, subtraction, multiplication, and division — based on user input.

---

## 📋 Features

- Accepts two numbers as input.
- Performs one of the following operations:
  - Addition (`+`)
  - Subtraction (`-`)
  - Multiplication (`*`)
  - Division (`/`)
- Displays the calculated result.
- Handles invalid operator input with an error message.

---

## 💻 How to Run

1. Make sure you have **Python 3** installed on your system.
2. Download or copy the script file: `simple calculator.py`
3. Open a terminal or command prompt in the directory containing the file.
4. Run the program using:
   ```bash
   python simple\ calculator.py




author 
Ankit
